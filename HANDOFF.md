# HANDOFF

> **เขียนทับทั้งไฟล์ทุกครั้งที่ส่งมอบ** ไม่ต่อท้าย — ไฟล์นี้คือ "ไม้ที่กำลังส่ง" ไม่ใช่ประวัติ
> ส่งมอบเมื่อ: 2026-09-22 (10:40) · เหตุผล: ยืนยันว่า cron เดินเองครบรอบแล้ว ไม่มีงานค้าง ผู้ใช้สั่งปิดเซสชัน

## ทำอะไรไปในเซสชันนี้

**22 ก.ย. (สั้น):** ตรวจ `events` ว่า cron เดินเองครบทุก job ✅ · แก้เวลาบนแผงที่ค้างอยู่ "08:00" → `CRON_TIMES` = `09:30 / ปิง 09:50 / ปิดวัน 23:50` + เทสกันลืม · ผู้ใช้กด `/panel` ยืนยันแล้วว่าขึ้นถูก

**21 ก.ย. (ก้อนใหญ่ 6 เรื่อง):**

1. cron 08:00 ยิงจริง 08:41 แต่ขนส่งอัปไฟล์ 08:44 → ย้าย cron ทั้งหมดไป cron-job.org · file id เดิมไม่เปลี่ยน ✅ ยืนยัน ADR-0003
2. แก้ `400 MAX_EMBED_SIZE_EXCEEDED` — `chunkEmbeds()` แบ่งข้อความตามตัวอักษรรวม 6000 ไม่ใช่แค่ 10 ใบ
3. **[ADR-0006] เลขประมูล** — `auction-rules.json` 301 เลข/หมวด จากตารางแนบท้ายประกาศฯ · แยกช่อง 🔨 · ช่องที่ 4 ในปุ่ม 🔢 ให้ทำเครื่องหมายเอง
4. **[ADR-0007] การ์ดประจำวัน 📣** — ใบเดียวต่อวัน 09:30 ลบเอง 23:50 · แทนการ์ด 🎯 รายวัน + ⏰ เตือนก่อนเปิด · มี "เลขที่ bot คัดให้" สายละ 3
5. แก้บั๊ก **id ของ Discord เพี้ยน** — `getMeta` อ่าน jsonb ผ่าน `JSON.parse` สองรอบ → snowflake 19 หลักกลายเป็น number → ลบข้อความผิดใบมาตลอดตั้งแต่ขึ้น Postgres
6. กวาดห้องอัตโนมัติ 09:30 (`DAILY_SWEEP=all`) + เคลียร์ DB เหลือเฉพาะของวันนี้

รายละเอียดเต็ม + เหตุผล: `docs/WORKLOG.md` › 2026-09-21 (4 หัวข้อ) และ 2026-09-22

## สถานะ ณ ตอนส่ง

- **git:** สะอาด · push แล้ว · ล่าสุด `4643929` · **115 เทสผ่าน · typecheck ผ่าน**
- **production:** `https://dlt-plate-watcher.vercel.app` Ready · env 7 ตัว (`DAILY_SWEEP=all` เพิ่ม 21 ก.ย.)
- **cron อยู่บน cron-job.org ทั้งหมด** (`vercel.json` ไม่มี `crons` แล้ว) — **ทดสอบแล้วว่าเดินเองครบทุก job เช้า 22 ก.ย.**
  | เวลา | URL | ทำอะไร |
  |---|---|---|
  | 09:30 | `/api/cron/daily` | กวาดห้อง → เช็คตาราง → การ์ดประจำวัน → แผง |
  | 09:50 | `/api/cron/ping` | 🚦 อีก 10 นาทีเปิดจอง |
  | 23:50 | `/api/cron/daily-clear` | ลบการ์ดประจำวัน |
- **Neon:** `notified` 3 · `wishlist` 13 (9 เลข + 🚫 4) · `meta` 4 · `events` 61 (id ล่าสุด 70)
- **ห้อง Discord:** 3 ข้อความ = 📣 การ์ด (09:30) · 🚦 ปิง (09:50) · 🏠 แผง — ตามดีไซน์
- **wishlist ปัจจุบัน:** `15` `24` `42` `45` `51` `54` `56` `65` `5456` (ผู้ใช้ลบ 8888/9999 เองคืน 21 ก.ย. หลังรู้ว่าเป็นเลขประมูล) · 🚫 `37` `73` `3773` `7337`

## ค้างอยู่ตรงไหน

**ไม่มีงานค้าง** — ทุกฟีเจอร์ทดสอบกับ production จริงและเห็น cron เดินเองแล้ว

## ทำต่อยังไง

1. **จ. 28 ก.ย. เช้า (นัดตรวจครั้งถัดไป): ตารางสัปดาห์ใหม่เข้าระบบถูกไหม**
   - `events` ต้องมี `notify` key `schedule:<Last-Modified ใหม่>` ตอน 09:30 (ขนส่งอัปไฟล์เช้าวันจันทร์ · 21 ก.ย. อัป 08:44)
   - การ์ดประจำวันต้องอ้างช่วงเลข/หมวดใหม่ · `meta.lastScheduleVersion` ต้องเปลี่ยนตาม
   - ถ้า **file id เปลี่ยน** (ยังไม่เคยเกิด) จะขึ้นข้อความ 🗓️ ตารางหมดอายุ → ผู้ใช้ต้องคัดลิงก์ iframe ใหม่มาใส่ `scheduleFileId` **ทั้งไฟล์และ `WATCH_CONFIG_JSON`**
2. **"ห้องเงียบ" ≠ "cron ตาย"** — `ping` ลง event เฉพาะตอนมีอะไรส่ง และ `sendDaily` ไม่ลง event เลยตอนไม่โพสต์
   (เสาร์-อาทิตย์ / ตาราง stale / วันที่ไม่มีเลขในฝันเปิด) → **ตรวจด้วย `npx vercel logs dlt-plate-watcher.vercel.app`**
   ซึ่งโชว์ `λ GET /api/cron/ping` พร้อมเวลา (ยืนยันมาแล้ว 23 ก.ย. 09:50:13 ว่ายิงจริงแต่ไม่มีเลขของผู้ใช้เปิดวันนั้น)
   · **ข้อเสนอที่ยังไม่ได้ทำ (ผู้ใช้ยังไม่สั่ง):** ให้ทุก endpoint เขียน `meta.lastRun:<job>` ทุกครั้งที่รันจบ
   จะได้ตรวจจาก DB ได้เลยไม่ต้องเปิด log · ไม่กวน 📜 เพราะอยู่ใน `meta` ไม่ใช่ `events`
3. "ทำไมการ์ดไม่ขึ้น" ให้ไล่ตามลำดับ: เสาร์-อาทิตย์? → ตาราง stale? → cron-job.org › History ยิงไหม? → `swept.blocked` เป็น true ไหม (สิทธิ์ Manage Messages หลุด) → `npx vercel logs dlt-plate-watcher.vercel.app`
4. งานที่คุยไว้แต่ผู้ใช้ยังไม่สั่ง: ตัด `events` เก็บย้อนหลัง 90 วันตอน 23:50 · เปลี่ยน pattern ใน wishlist ที่ชนกลุ่มประมูล · drawio หน้า 01–07 ยังเป็นภาพก่อนมีเลขประมูล/การ์ดประจำวัน

## สิ่งที่ตกลงกันไว้แต่ยังไม่ได้เขียนลงไฟล์ไหน

- **`DAILY_SWEEP=all` อยู่บน Vercel เท่านั้น ไม่มีในรีโป** — ลบค่า env ทิ้ง = กลับไปกวาดเฉพาะข้อความของ bot ทันทีโดยไม่ต้อง deploy · ผู้ใช้เลือกเอง ("ลบทุกอย่างให้ห้องโล่งจริง") และรับรู้ว่ามันลบข้อความของคนอื่นในช่องด้วย
- **ปิด Vercel Cron 08:00 = ยอมเสียตัวสำรอง** ผู้ใช้รับทราบว่าถ้า cron-job.org ล่มจะไม่มีอะไรยิงเลย · ถ้าจะเอาคืน ทางที่เสนอไว้คือเปลี่ยนการกวาดเป็น "ลบเฉพาะข้อความที่เก่ากว่าวันนี้" แล้วเปิด cron 08:00 กลับ
- **pattern ที่เหลือใน wishlist (เลขตอง · เลขคู่สลับ) คือกลุ่มเลขประมูลทั้งกลุ่ม** → ให้เลขที่จองได้ = 0 ตลอดไป (ตอนนี้ที่ใช้งานได้จริงคือ `numbers` 9 เลข)
  ทางเลือกที่เสนอไว้และ **ผู้ใช้ยังไม่ตัดสินใจ**: `^(\d)\1\1\d$` (1115) · `^\d\d99$` (1299) · เลขเรียงถอยหลัง (9876 4321) · `digitSums`
  แก้ pattern ต้องแก้ **ทั้งสองที่**: `watch.config.json` บนเครื่อง **และ** `WATCH_CONFIG_JSON` บน Vercel (env นี้เป็น snapshot ของไฟล์วันที่ 20 ก.ย. ยังมี `reminders.daysBeforeOpen` ที่เลิกใช้ — Zod เมินให้เอง ไม่ error)
- **ปุ่ม 🧹 ตั้งใจให้ลบเฉพาะข้อความของ bot เสมอ** ไม่ว่าจะตั้ง `DAILY_SWEEP` ไว้ยังไง — คนกดปุ่มไม่ควรเผลอลบข้อความคนอื่น **อย่า "แก้ให้สอดคล้องกัน"**
- **ข้อความเวลา cron บนแผงอยู่ที่ const `CRON_TIMES`** ใน `notify/discord.ts` ที่เดียว มีเทสล็อกไว้ — ย้ายเวลา cron แล้วต้องแก้ตรงนี้ด้วย
- **`npm run gen:auction` ดึง PDF จาก tabienrod.com** — สคริปต์ตอนพัฒนา ไม่ใช่ runtime จึงไม่ขัด ADR-0003 · ถ้ารันแล้วได้ไม่ครบ 301 สคริปต์จะล้มเอง **อย่าไปแก้ตัวเลขให้ผ่าน**
- **`tsconfig.json` ไม่ได้ include `tests/`** → เทสไม่ถูก typecheck (vitest แค่ transpile) เคยทำให้เรียกฟังก์ชันด้วยจำนวน argument ผิดแล้วไม่มีใครรู้ · ถ้าจะแก้ ระวัง config literal ในเทสหลายไฟล์จะแดงพร้อมกัน
- **ไฟล์สำรอง DB ก่อนเคลียร์** (4 ตาราง 95 แถว) อยู่ใน scratchpad ของเซสชันที่จบแล้ว — **ถือว่าหายได้** · ห้าม commit ไฟล์แบบนี้ (มี Discord user id)
- **ผู้ใช้ชอบให้ verify กับของจริงเสมอ** — แก้อะไรแล้วให้ยิง endpoint จริงและอ่านกลับจาก Discord API / ตาราง `events` มาแสดง ไม่ใช่บอกว่า "น่าจะทำงานได้" · เจอว่าตัวเองสรุปผิดให้บอกตรง ๆ (21 ก.ย. ผมเดาผิดว่าแผงค้างเพราะ 429 แล้วแก้คำพูดทีหลัง)

## เกณฑ์ว่าไม้นี้ส่งได้จริง

เปิดแชตใหม่ อ่านไฟล์นี้ + `HOTCACHE.md` แล้วเมื่อผู้ใช้บอกว่า "เช็คตารางใหม่ให้หน่อย" (จันทร์) หรือ "การ์ดไม่ขึ้น"
รู้ทันทีว่าต้องดูอะไรก่อน–หลัง · และรู้ว่าห้ามแตะอะไร (ปุ่ม 🧹 · ตัวเลข 301 · `wishlist`/`meta` ในฐานข้อมูล)
