# HANDOFF

> **เขียนทับทั้งไฟล์ทุกครั้งที่ส่งมอบ** ไม่ต่อท้าย — ไฟล์นี้คือ "ไม้ที่กำลังส่ง" ไม่ใช่ประวัติ
> ส่งมอบเมื่อ: 2026-09-24 (เย็น) · เหตุผล: **ผู้ใช้จองเลขได้จริง** + แก้ ⚠️ เตือนจดทะเบียนที่ไม่เคยทำงานจนใช้ได้แล้ว ไม่มีงานค้าง

## ทำอะไรไปในเซสชันนี้ (24 ก.ย.)

ตรวจผล cron ประจำวัน → ผู้ใช้แจ้งว่าจองเลขได้ → พบว่า ⚠️ เตือนจดทะเบียนไม่เคยทำงาน →
**[ADR-0008] ปุ่ม 🏆 จองได้แล้ว + `meta.won`** (128 เทส) → บันทึก `8ขช 5456` ให้ → เปลี่ยนวันเตือนเป็น `[7,3,1,0]`
ทั้งไฟล์และ env บน Vercel · รายละเอียดเต็ม: `docs/WORKLOG.md` › 2026-09-24 (2 หัวข้อ)

## 🎉 เหตุการณ์สำคัญ: 24 ก.ย. ผู้ใช้จองได้ `8ขช 5456`

เป้าหมายของโปรเจกต์สำเร็จครั้งแรก · ทางที่ระบบพาไปถึง: 09:30 การ์ดประจำวันขึ้น `mine: 1` → 09:50 🚦 ปิง (`sent: 1`)
→ 10:00 ผู้ใช้กดจองเอง (ADR-0001 — bot ไม่จองแทน) · ผู้ใช้ลบ `5456` ออกจาก wishlist เองแล้ว เหลือ 8 เลข

## ✅ แก้แล้ว: ⚠️ เตือนหมดเขตจดทะเบียน (ADR-0008)

ของเดิมคำนวณจาก `schedule.entries` ของสัปดาห์ปัจจุบัน → แถวของวันที่จองได้หายจาก PDF ก่อนถึงกำหนดเสมอ **จึงไม่เคยยิงเลย**
ตอนนี้ย้ายมาเก็บเป็นรายการของตัวเองที่ **`meta.won`** ผ่าน **ปุ่ม 🏆 จองได้แล้ว** (modal 4 ช่อง · ไม่ต้อง migration)

- `8ขช 5456` **บันทึกไว้แล้ว** (จดภายใน 26 ต.ค. 2569) — ผู้ใช้ขอให้บันทึกให้จากเครื่องผ่านเส้นทางเดียวกับปุ่ม
- `reminders.daysBeforeRegisterDeadline` เปลี่ยนเป็น **`[7, 3, 1, 0]`** (แก้ทั้งไฟล์และ env บน Vercel)
  → จะเตือน **19 · 23 · 25 · 26 ต.ค.** · วันสุดท้ายขึ้นเป็น 🚨 "วันนี้วันสุดท้าย"
- จดทะเบียนเสร็จแล้ว กด 🏆 ใส่เลขในช่องล่างสุดเพื่อหยุดเตือน (หรือปล่อยไว้ก็หยุดเองหลังเลยกำหนด)

## สถานะ ณ ตอนส่ง

- **git:** สะอาด · push แล้ว · **128 เทสผ่าน · typecheck ผ่าน** · deploy Ready
- **production:** `https://dlt-plate-watcher.vercel.app` Ready · env 7 ตัว (`DAILY_SWEEP=all`)
- **cron อยู่บน cron-job.org ทั้งหมด** (`vercel.json` ไม่มี `crons`) · **เดินเองครบทุกวัน 22–24 ก.ย. ไม่มีพลาดสักรอบ**
  | เวลา | URL | ทำอะไร |
  |---|---|---|
  | 09:30 | `/api/cron/daily` | กวาดห้อง → เช็คตาราง → การ์ดประจำวัน → แผง |
  | 09:50 | `/api/cron/ping` | 🚦 อีก 10 นาทีเปิดจอง (เฉพาะวันที่มีเลขในฝันเปิด) |
  | 23:50 | `/api/cron/daily-clear` | ลบการ์ดประจำวัน |
- **Neon:** `notified` 4 · `wishlist` 12 (8 เลข + 🚫 4) · `meta` 5 (เพิ่ม `won`) · `events` 78
- **wishlist ปัจจุบัน:** `15` `24` `42` `45` `51` `54` `56` `65` · 🚫 `37` `73` `3773` `7337`

## ค้างอยู่ตรงไหน

**ไม่มีงานค้าง** — ⚠️ แก้เสร็จและบันทึกเลขของผู้ใช้ไว้แล้ว · เหลือแค่รอดูว่าเตือนมาจริงวันที่ 19 ต.ค.

## ทำต่อยังไง

1. **จ. 28 ก.ย. เช้า: ตารางสัปดาห์ใหม่เข้าระบบถูกไหม** — `events` ต้องมี `notify` key `schedule:<Last-Modified ใหม่>`
   ตอน 09:30 · การ์ดต้องอ้างหมวด/ช่วงเลขใหม่ · `meta.lastScheduleVersion` เปลี่ยนตาม
   ถ้า **file id เปลี่ยน** (ยังไม่เคยเกิด) จะขึ้น 🗓️ ตารางหมดอายุ → ผู้ใช้ต้องคัดลิงก์ iframe ใหม่มาใส่ **ทั้งไฟล์และ `WATCH_CONFIG_JSON`**
2. **19 ต.ค.: ⚠️ เตือนจดทะเบียนใบแรกต้องมาจริง** (`events` kind `notify` key `won:8ขช:5456:2026-10-26:7`) — ใบแรกที่ฟีเจอร์นี้เคยยิงได้
3. **"ห้องเงียบ" ≠ "cron ตาย"** — `ping`/`sendDaily` ลง event เฉพาะตอนมีอะไรส่ง (เสาร์-อาทิตย์เงียบสนิท = ปกติ)
   ตรวจด้วย `npx vercel logs dlt-plate-watcher.vercel.app` ซึ่งโชว์ `λ GET /api/cron/ping` พร้อมเวลา
   · ข้อเสนอที่ยังไม่ได้ทำ: เขียน `meta.lastRun:<job>` ทุกครั้งที่รันจบ จะได้ตรวจจาก DB ได้เลย
4. "ทำไมการ์ดไม่ขึ้น": เสาร์-อาทิตย์? → ตาราง stale? → cron-job.org › History → `swept.blocked` (สิทธิ์ Manage Messages) → Vercel logs
5. งานที่คุยไว้แต่ยังไม่สั่ง: ตัด `events` เก็บ 90 วัน · เปลี่ยน pattern ที่ชนกลุ่มประมูล · drawio หน้า 01–07 ยังเป็นภาพเก่า

## สิ่งที่ตกลงกันไว้แต่ยังไม่ได้เขียนลงไฟล์ไหน

- **`DAILY_SWEEP=all` อยู่บน Vercel เท่านั้น ไม่มีในรีโป** — ลบค่า env ทิ้ง = กลับไปกวาดเฉพาะของ bot ทันทีโดยไม่ต้อง deploy · ผู้ใช้เลือกเอง และรู้ว่ามันลบข้อความของคนอื่นในช่องด้วย
- **ปิด Vercel Cron 08:00 = ยอมเสียตัวสำรอง** ถ้า cron-job.org ล่มจะไม่มีอะไรยิงเลย · จะเอาคืนต้องเปลี่ยนการกวาดเป็น "ลบเฉพาะที่เก่ากว่าวันนี้" ก่อน
- **pattern ที่เหลือใน wishlist (เลขตอง · เลขคู่สลับ) คือกลุ่มเลขประมูลทั้งกลุ่ม** → ให้เลขที่จองได้ = 0 เสมอ · ที่ใช้งานได้จริงคือ `numbers` 8 เลข
  ทางเลือกที่เสนอไว้: `^(\d)\1\1\d$` (1115) · `^\d\d99$` (1299) · เลขเรียงถอยหลัง (9876 4321) · `digitSums`
  แก้ pattern ต้องแก้ **ทั้งสองที่**: `watch.config.json` **และ** `WATCH_CONFIG_JSON` บน Vercel
  · **`vercel env pull` อ่านค่าเดิมกลับไม่ได้** (คืน `[SENSITIVE]`) → ต้องประกอบใหม่จากไฟล์บนเครื่องแล้ว `rm` + `add` ทั้ง production และ preview แล้ว push ให้ deploy ใหม่ env ถึงมีผล
- **ปุ่ม 🧹 ตั้งใจให้ลบเฉพาะข้อความของ bot เสมอ** ไม่ว่าตั้ง `DAILY_SWEEP` ไว้ยังไง **อย่า "แก้ให้สอดคล้องกัน"**
- **ข้อความเวลา cron บนแผงอยู่ที่ const `CRON_TIMES`** ใน `notify/discord.ts` ที่เดียว มีเทสล็อกไว้
- **`npm run gen:auction` ดึง PDF จาก tabienrod.com** — สคริปต์ตอนพัฒนา ไม่ใช่ runtime · ได้ไม่ครบ 301 ต้องล้ม **อย่าแก้ตัวเลขให้ผ่าน**
- **`tsconfig.json` ไม่ได้ include `tests/`** → เทสไม่ถูก typecheck · จะแก้ต้องเตรียมเจอ config literal ในเทสแดงพร้อมกันหลายไฟล์
- **ผู้ใช้ชอบให้ verify กับของจริงเสมอ** — ยิง endpoint จริงแล้วอ่านกลับจาก Discord API / ตาราง `events` มาแสดง · สรุปผิดให้บอกตรง ๆ (เซสชันนี้ผมพลาด 2 ครั้ง: เดาว่าแผงค้างเพราะ 429 · บอกว่า ⚠️ จะเตือนหมดเขตให้เองทั้งที่ยังไม่ได้แก้)

## เกณฑ์ว่าไม้นี้ส่งได้จริง

เปิดแชตใหม่ อ่านไฟล์นี้ + `HOTCACHE.md` แล้วเมื่อผู้ใช้ถามว่า "เช็คตารางใหม่" (จันทร์) หรือ "เตือนมาไหม" (19 ต.ค.)
รู้ทันทีว่าต้องดู `events` อะไรก่อน–หลัง · รู้ว่า ⚠️ มาจาก `meta.won` ไม่ใช่ตาราง
และรู้ว่าห้ามแตะอะไร (ปุ่ม 🧹 · ตัวเลข 301 · `wishlist`/`meta` ในฐานข้อมูล)
