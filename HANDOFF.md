# HANDOFF

> **เขียนทับทั้งไฟล์ทุกครั้งที่ส่งมอบ** ไม่ต่อท้าย — ไฟล์นี้คือ "ไม้ที่กำลังส่ง" ไม่ใช่ประวัติ
> ส่งมอบเมื่อ: 2026-09-21 (เที่ยง) · อัปเดต 2026-09-22 เช้า (ยืนยันผล cron + แก้เวลาบนแผง)

## ทำอะไรไปในเซสชันนี้

1. **ยืนยัน cron 08:00** — ยิงจริง 08:41 แต่ขนส่งอัปไฟล์ 08:44 (พลาด 3 นาที) · file id เดิมไม่เปลี่ยน ✅ ยืนยัน ADR-0003
2. **แก้ 400 MAX_EMBED_SIZE_EXCEEDED** — `chunkEmbeds()` แบ่งข้อความตามตัวอักษรรวม 6000 ไม่ใช่แค่ 10 ใบ
3. **[ADR-0006] เลขประมูล** — `auction-rules.json` 301 เลข/หมวด จากตารางแนบท้ายประกาศฯ · แยกช่อง 🔨 · ช่องที่ 4 ในปุ่ม 🔢 ให้ทำเครื่องหมายเอง
4. **[ADR-0007] การ์ดประจำวัน 📣** — ใบเดียวต่อวัน 09:30 ลบเอง 23:50 · แทนการ์ด 🎯 รายวัน + ⏰ เตือนก่อนเปิด · มี "เลขที่ bot คัดให้" สายละ 3
5. **แก้บั๊ก id เพี้ยน** — `getMeta` อ่าน jsonb ผ่าน JSON.parse สองรอบ → snowflake 19 หลักกลายเป็น number → **ลบข้อความผิดใบมาตลอดตั้งแต่ขึ้น Postgres**
6. **กวาดห้องอัตโนมัติ 09:30** + เคลียร์ DB ให้เหลือเฉพาะของวันนี้

รายละเอียดเต็ม + เหตุผล: `docs/WORKLOG.md` › 4 หัวข้อของวันที่ 2026-09-21

## สถานะ ณ ตอนส่ง

- **git:** สะอาด · push แล้ว (ล่าสุด `338b1bf`) · **115 เทสผ่าน · typecheck ผ่าน**
- **production:** `https://dlt-plate-watcher.vercel.app` · env **7 ตัว** (เพิ่ม `DAILY_SWEEP=all` วันนี้)
- **cron ทั้งหมดอยู่บน cron-job.org** (Vercel Cron ถูกปิดแล้ว · `vercel.json` ไม่มี `crons`)
  | เวลา | URL | ทำอะไร |
  |---|---|---|
  | 09:30 | `/api/cron/daily` | กวาดห้อง → เช็คตาราง → การ์ดประจำวัน → แผง |
  | 09:50 | `/api/cron/ping` | 🚦 อีก 10 นาทีเปิดจอง |
  | 23:50 | `/api/cron/daily-clear` | ลบการ์ดประจำวัน |
- **Neon:** `notified` 2 · `wishlist` 15 · `meta` 4 · `events` 51 (ทั้งหมดเป็นของวันที่ 21 ก.ย.)
- **ห้อง Discord:** เหลือ 2 ข้อความ — 📣 การ์ดวันนี้ (12:10) + 🏠 แผง
- ทุกฟีเจอร์ทดสอบกับ production จริงแล้ว (ยิง endpoint เองแล้วอ่านกลับจาก Discord API + ตาราง `events`)

## ค้างอยู่ตรงไหน

**ไม่มีงานค้าง** · ✅ **22 ก.ย. เช้า: cron ทำงานเองครบทุก job แล้ว** (23:50 clear · 09:30 กวาด+เช็ค+การ์ด `mine:8` · 09:50 ping)
ห้องเหลือ 3 ข้อความตามดีไซน์ · ผู้ใช้ลบ 8888/9999 ออกจาก wishlist เองเมื่อคืน (เหลือ 9 เลขที่จองได้ทั้งหมด)

## ทำต่อยังไง

1. **จ. 28 ก.ย. เช้า: ดูว่าตารางสัปดาห์ใหม่เข้าระบบถูกไหม** — `events` ต้องมี `notify` key `schedule:<Last-Modified ใหม่>`
   ตอน 09:30 (ขนส่งอัปไฟล์เช้าวันจันทร์ · 21 ก.ย. อัป 08:44) และการ์ดประจำวันต้องอ้างช่วงเลขของหมวดใหม่
2. ถ้าผู้ใช้ถามว่า "ทำไมการ์ดไม่ขึ้น" ให้ไล่ตามลำดับ: เสาร์-อาทิตย์? → ตาราง stale? → cron-job.org ยิงไหม? → `swept.blocked` เป็น true ไหม (สิทธิ์ Manage Messages หลุด)
3. งานที่คุยไว้แต่ยังไม่ได้ทำ (ผู้ใช้ยังไม่สั่ง): ตัด `events` อัตโนมัติเก็บย้อนหลัง 90 วันตอน 23:50 · drawio หน้า 01–07 ยังเป็นภาพก่อนมีการ์ดประจำวัน/เลขประมูล

## สิ่งที่ตกลงกันไว้แต่ยังไม่ได้เขียนลงไฟล์ไหน

- **`DAILY_SWEEP=all` อยู่บน Vercel เท่านั้น ไม่มีในรีโป** — ลบค่า env ทิ้ง = กลับไปกวาดเฉพาะข้อความของ bot ทันทีโดยไม่ต้อง deploy · ผู้ใช้เลือกเอง ("ลบทุกอย่างให้ห้องโล่งจริง") และรับรู้ว่ามันลบข้อความของคนอื่นในช่องด้วย
- **ปิด Vercel Cron 08:00 = ยอมเสียตัวสำรอง** ผู้ใช้รับทราบแล้วว่าถ้า cron-job.org ล่มจะไม่มีอะไรยิงเลย · ถ้าจะเอาตัวสำรองคืน ทางที่เสนอไว้คือเปลี่ยนการกวาดเป็น "ลบเฉพาะข้อความที่เก่ากว่าวันนี้" แล้วเปิด cron 08:00 กลับ
- **pattern ใน wishlist ของผู้ใช้ (เลขตอง · เลขคู่สลับ) คือกลุ่มเลขประมูลทั้งกลุ่ม** → ได้เลขที่จองได้ = 0 ตลอดไป
  ผมเสนอทางเลือกที่ไม่ชนกลุ่มประมูลไว้แล้ว (`^(\d)\1\1\d$` 1115 · `^\d\d99$` 1299 · เลขเรียงถอยหลัง 9876 4321 · digitSums) **ผู้ใช้ยังไม่ตัดสินใจ** — ถ้าจะแก้ต้องแก้**ทั้งสองที่**: `watch.config.json` บนเครื่อง **และ** `WATCH_CONFIG_JSON` บน Vercel (env ตัวนี้เป็น snapshot ของไฟล์วันที่ 20 ก.ย. ยังมี `reminders.daysBeforeOpen` ที่เลิกใช้แล้วอยู่ — Zod เมินให้เอง ไม่ error)
- **ปุ่ม 🧹 ตั้งใจให้ลบเฉพาะข้อความของ bot เสมอ** ไม่ว่าจะตั้ง `DAILY_SWEEP` ไว้ยังไง — คนกดปุ่มไม่ควรเผลอลบข้อความคนอื่น อย่า "แก้ให้สอดคล้องกัน"
- **ไฟล์สำรอง DB ก่อนเคลียร์** (4 ตาราง 95 แถว) อยู่ที่ `/private/tmp/claude-501/.../scratchpad/neon-backup-2026-09-21.json` — โฟลเดอร์ชั่วคราวของเซสชันที่จบไปแล้ว **ถือว่าหายได้** · ห้าม commit ไฟล์แบบนี้ (มี Discord user id)
- **`npm run gen:auction` ดึง PDF จาก tabienrod.com** — เป็นสคริปต์ตอนพัฒนา ไม่ใช่ runtime จึงไม่ขัด ADR-0003 · ถ้ารันแล้วได้ไม่ครบ 301 สคริปต์จะล้มเอง อย่าไปแก้ตัวเลขให้ผ่าน
- **`tsconfig.json` ไม่ได้ include `tests/`** → เทสไม่ถูก typecheck (vitest รันได้เพราะ transpile อย่างเดียว) เคยทำให้เรียกฟังก์ชันด้วยจำนวน argument ผิดแล้วไม่มีใครรู้ · ถ้าจะแก้ ระวัง type ของ config literal ในเทสหลายไฟล์จะแดงขึ้นมาพร้อมกัน
- **ผู้ใช้ชอบให้ verify กับของจริงเสมอ** — ทุกครั้งที่แก้ ให้ยิง endpoint จริงแล้วอ่านกลับจาก Discord API/ตาราง `events` มาแสดง ไม่ใช่บอกว่า "น่าจะทำงานได้" · และเวลาเจอว่าตัวเองสรุปผิด ให้บอกตรง ๆ (วันนี้ผมเดาผิดว่าแผงค้างเพราะ 429 แล้วแก้คำพูดทีหลัง)

## เกณฑ์ว่าไม้นี้ส่งได้จริง

เปิดแชตใหม่ อ่านไฟล์นี้ + `HOTCACHE.md` แล้วเมื่อผู้ใช้บอกว่า "เช็คให้หน่อยว่าเมื่อเช้าทำงานไหม"
รู้ทันทีว่าต้องดู `events` 4 แถวไหน · ถ้าไม่ครบต้องไปดู cron-job.org กับ Vercel logs ตรงไหน
และรู้ว่าห้ามแตะอะไร (ปุ่ม 🧹 · ตัวเลข 301 · wishlist/meta ในฐานข้อมูล)
